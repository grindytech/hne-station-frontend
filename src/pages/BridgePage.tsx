import { HStack } from "@chakra-ui/react";
import Bridge from "components/bridge/\bBridge";

export default function BridgePage() {
  return (
    <HStack mt={[10, 5]} justifyContent="center">
      <Bridge />
    </HStack>
  );
}
